<dl>
	<dt><?= !empty($name) ? $name : "" ?></dt>
	<dd>
		<blockquote><?= !empty($quote) ? $quote : "" ?>
		<?= !empty($cite) ? "<cite>".$cite."</cite>" : "" ?> 
		</blockquote>
		<?= !empty($comment) ? $comment : ""?>
	</dd>
</dl>